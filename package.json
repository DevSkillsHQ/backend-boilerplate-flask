{
  "name": "backend-boilerplate-flask",
  "version": "1.0.0",
  "scripts": {
    "build": "cd app && pip3 install -r requirements.txt",
    "start": "cd app && export FLASK_APP=main.py && python3 -m flask run",
    "test": "cypress run"
  },
  "devDependencies": {
    "cypress": "^14.5.3"
  }
}